import{notFound}from"next/navigation";import{AboutPage}from"@/components/pages/standard-pages";import{isLocale}from"@/lib/i18n";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();return <AboutPage locale={locale}/>}
