import type{Metadata}from"next";import{notFound}from"next/navigation";import{LocalizedProjectPage}from"@/components/pages/project-page";import{getProjectBySlug}from"@/repositories/projects";import{isLocale,localizeProject}from"@/lib/i18n";
export async function generateMetadata({params}:{params:Promise<{locale:string;slug:string}>}):Promise<Metadata>{const{locale,slug}=await params;if(!isLocale(locale))return{};const raw=await getProjectBySlug(slug);if(!raw)return{};const p=localizeProject(raw,locale);return{title:p.title,description:p.shortDescription,alternates:{languages:{sr:`/sr/projekti/${slug}`,en:`/en/projekti/${slug}`}}}}
export default async function Page({params}:{params:Promise<{locale:string;slug:string}>}){const{locale,slug}=await params;if(!isLocale(locale))notFound();return <LocalizedProjectPage slug={slug} locale={locale}/>}
