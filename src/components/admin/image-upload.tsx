"use client";
import { useState } from "react";
export function ImageUpload({name,label,defaultValue=""}:{name:string;label:string;defaultValue?:string}){const [value,setValue]=useState(defaultValue),[busy,setBusy]=useState(false),[error,setError]=useState("");async function upload(file:File){setBusy(true);setError("");const data=new FormData();data.set("file",file);const response=await fetch("/api/admin/upload",{method:"POST",body:data});const result=await response.json();setBusy(false);if(!response.ok){setError(result.error||"Upload nije uspeo.");return;}setValue(result.path);}return <div className="form-field"><label>{label}</label><input type="hidden" name={name} value={value}/><input value={value} onChange={e=>setValue(e.target.value)} placeholder="/uploads/... ili https://"/><input type="file" accept="image/jpeg,image/png,image/webp,image/avif" onChange={e=>e.target.files?.[0]&&upload(e.target.files[0])}/>{busy&&<small>Otpremanje…</small>}{error&&<small className="text-red-700">{error}</small>}</div>}
