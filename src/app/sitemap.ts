import type { MetadataRoute } from "next";
import { getProjects } from "@/repositories/projects";
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000";const routes=["","/prodaja-stanova","/poslovni-prostori","/lux-stanovi","/izvedeni-projekti","/o-nama","/kontakt"];const projects=(await getProjects()).filter(p=>p.status!=="DRAFT");return ["sr","en"].flatMap(locale=>[...routes.map(route=>({url:`${base}/${locale}${route}`,lastModified:new Date(),changeFrequency:"weekly" as const,priority:route===""?1:.8,alternates:{languages:{sr:`${base}/sr${route}`,en:`${base}/en${route}`}}})),...projects.map(p=>({url:`${base}/${locale}/projekti/${p.slug}`,lastModified:new Date(),changeFrequency:"monthly" as const,priority:.7,alternates:{languages:{sr:`${base}/sr/projekti/${p.slug}`,en:`${base}/en/projekti/${p.slug}`}}}))]);}
